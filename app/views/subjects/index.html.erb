<h1><%= link_to(h(@bibliome.query), bibliome_path(@bibliome)) %></h1>

<h2>Subjects</h2>
<ol>
<% @subjects.each do |s| -%>
  <li><%= link_to(s.subject.term, bibliome_subject_path(@bibliome, s.subject)) %></li>
<% end -%>
</ol>