<h2>Subjects</h2>
<%= page_entries_info @subjects %>
<ol>
<% @subjects.each do |s| -%>
  <li><%= link_to(s.subject.term, bibliome_subject_path(@bibliome, s.subject)) %> <span class="count">(<%= s.all %>)</span></li>
<% end -%>
</ol>
<%= will_paginate @subjects %>