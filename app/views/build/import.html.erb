Background job added!

<table>
  <tr>
    <th>ID</th>
    <th>PRIORITY</th>
    <th>JOB</th>
    <th>RUN AT</th>
    <th>CREATED AT</th>
  </tr>
<% Delayed::Job.find(:all).each do |j| %>
  <tr>
    <td><%= j.id %></td>
    <td><%= j.priority%></td>
    <td><%= j.handler %></td>
    <td><%= j.run_at %></td>
    <td><%= j.created_at %></td>
  </tr>
<% end %>
</table>